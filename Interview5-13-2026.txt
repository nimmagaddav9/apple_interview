Jeff Bell
Introduce yourself
visual consistentcy at apple
ease of development
experince in component libraries
webcompnents libraries
waht are up and downs in an projects I worked
expericene with ai
we told about copolit tell me more to impress here
we do have claude code in apple
what is mcp and agents
if they asked have used mcp before
did u built any agent?

coding questions
Q1.  what is the output?
code --> what is the output(3 questions);
1
1

========
1
0
=========
Inside function → local count becomes 1 → prints 1
Outside function → global count is still 0 → prints 0


Q2. console.log('1');

setTimeout(() => {
    console.log('2');
}, 0);

const p = new Promise((resolve) => resolve());

p.then(() => console.log('3'));

(() => console.log('4'))();

1 → runs immediately
4 → IIFE runs immediately
3 → Promise (microtask) runs next
2 → setTimeout (macrotask) runs last
So answer: 1 → 4 → 3 → 2 ✅

// Problem Statement:
// ------------------
// Create an HTML input that accepts a 10 digit telephone number
// 
//
// Example:
// Example:
// --------
//
// User types:  1234567890
// Input shows: (123) 456-7890 -->

index.html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto">
    <script type="module" src="/src/main.js"></script>
    <title>HTML | CoderPad</title>
  </head>
  <body>
   <input type="text" id="phone" maxlength="14" placeholder="(123) 456-7890"/>
  </body>
</html>

main.js
========================================
import './style.css'

console.log('Edit src/main.js to get started...')

const input = document.getElementById("phone");

input.addEventListener("input",(e)=>{
  let value = e.target.value.replace(/\D/g,"");
  value = value.substring(0,10);
  let formatted = "";

  if(value.length > 0){
    formatted +="("+ value.substring(0,3); 
  }
   if(value.length >= 4){
        formatted +=") "+ value.substring(3,6); 

  }
   if (value.length >= 7){
            formatted +="-"+ value.substring(6,10); 

  }
  e.target.value = formatted;
})

//
